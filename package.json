{
  "scripts": {
    "now-build": "next build && next export -o dist",
    "now-dev": "next -p $PORT"
  },
  "dependencies": {
    "buffer": "^5.2.1",
    "next": "latest",
    "react": "latest",
    "react-dom": "latest",
    "u2f-api": "^1.0.10"
  },
  "devDependencies": {
    "eslint": "^5.16.0",
    "eslint-plugin-react": "^7.13.0"
  }
}
